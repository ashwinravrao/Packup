<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/background_window</item>
        <item name="drawerArrowStyle">@style/AppTheme.DrawerArrowStyle</item>
        <item name="android:colorEdgeEffect">@color/filter_image_preview</item>

    </style>

    <style name="AppTheme.DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/iconAndTextColor</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.PopUpOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:colorBackground">@android:color/white</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="AppTheme.AppBarOverlay.Black" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:actionMenuTextColor">@android:color/white</item>
    </style>

    <style name="HintText" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/top_row_view_holder</item>
    </style>

    <style name="TextInputLayoutAppearance" parent="Base.Widget.MaterialComponents.TextInputLayout">
        <item name="android:textColor">@color/top_row_view_holder</item>
        <item name="android:textColorHint">@color/top_row_view_holder</item>
        <item name="hintTextAppearance">@style/HintText</item>
        <item name="boxStrokeColor">@color/colorAccent</item>
        <item name="android:colorControlActivated">@color/top_row_view_holder</item>
        <item name="android:colorControlNormal">@color/top_row_view_holder</item>
        <item name="android:colorControlHighlight">@color/colorAccent</item>
    </style>

</resources>
